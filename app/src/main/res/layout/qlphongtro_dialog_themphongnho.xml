<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFE5E5"
    android:gravity="center">

    <!-- Hộp thoại chính -->
    <LinearLayout
        android:id="@+id/dialogContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="8dp"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingBottom="8dp"
            android:text="Nhập thông tin phòng"
            android:textSize="25dp"
            android:textColor="@color/purple"
            android:textStyle="bold" />

        <!-- Tên phòng -->

        <!-- Giá phòng -->
        <EditText
            android:id="@+id/edtTenPhong_themphong"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:autofillHints="name"
            android:background="@drawable/bg_border_green_circle"
            android:hint="Tên phòng"
            android:textColor="@android:color/white"
            android:inputType="textCapWords"
            android:minHeight="48dp"
            android:padding="10dp" />

        <EditText
            android:id="@+id/edtGiaPhong_themphong"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/bg_border_green_circle"
            android:autofillHints="price"
            android:hint="Giá phòng"
            android:inputType="number"
            android:minHeight="48dp"
            android:padding="10dp" />

        <!-- Nút hành động -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="end"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnHuy_themPhong"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="12dp"
                android:backgroundTint="#099578"
                android:text="Hủy" />

            <Button
                android:id="@+id/btnLuu_themPhong"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:backgroundTint="#099578"
                android:text="Lưu" />
        </LinearLayout>

    </LinearLayout>
</FrameLayout>
