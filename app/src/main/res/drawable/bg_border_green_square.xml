<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="#CEFFCF" />

    <!-- Viền -->
    <stroke
        android:width="2dp"
        android:color="#18FF00" /> <!-- màu viền -->

    <!-- Bo góc -->
    <corners android:radius="5dp" />

    <!-- Padding bên trong -->
    <padding
        android:left="8dp"
        android:top="4dp"
        android:right="8dp"
        android:bottom="4dp" />
</shape>